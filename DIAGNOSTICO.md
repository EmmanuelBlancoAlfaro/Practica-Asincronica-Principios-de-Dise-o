# Diagnóstico del código de partida

Lea `clinicasegura/legado.py` entero antes de escribir una sola línea de
código nuevo. Llene una fila por principio. En la columna de evidencia
cite **archivo y línea** (por ejemplo `legado.py:38`); una fila sin
evidencia no cuenta.

Si cree que un principio **no** está violado, escriba la fila igual y
explique por qué en la columna de hallazgo.

| # | Principio | Hallazgo concreto | Evidencia (archivo:línea) | Qué cuesta si no se corrige |
|---|-----------|-------------------|---------------------------|------------------------------|
| 1 | Dividir y conquistar |La clase ServicioRecetas es la que literalmente hace todo, validar, calcular, hablar HTTP, escribir en la base de datos, generar folios y registrar en la bitacora |(legado.py:44)|Si no se resuelve esto va a tender a errores al sobre acumular trabajo en una sola clase. |
| 2 | Aumentar la cohesión |Al igual que el primero la clase ServicioRecetas hace todo, incumpliendo este principio por completo |(legado.py:44)|Pasará exactamente lo mismo que en el primer caso, van ha comenzar a causar errores al sobre acumular trabajo en una sola clase.|
| 3 | Reducir el acoplamiento |1. En el CONFIG se encuentra el primer problema que es una variable global, haciendo que el principio se rompa. 2. La función "reporte()", en la clase ServicioRecetas, rompe este principio al tener como parametro todo el paciente, en vez de solo obtener los datos que se desean |1. (legado.py:30). 2.(legado.py:146)|1. Si no corregimos que la variable sea global, esto ocasionaria problemas impredescibles, ya que cualquiera podria obtener las API's y usarlas. 2. Si no se arregla el recibir un objeto completo este va a arrastrar dependencias innecesarias en las firmas de los métodos |
| 4 | Mantener alta la abstracción |Tener código SQL crudo en una función rompe el principio de abstracción alta. Esto se observa en el método de emitir(), mas especifico en el self.db.execute. |(legado.py:98)|Al tener las sentencias de SQL en crudo esto se romperá si se llega a cambiar de SQLite a Postgres, por ejemplo, haciendo que tengamos que cambiar toda la función, cosa que no pasaria si no tuvieramos este codigo crudo.|
| 5 | Aumentar la reusabilidad |En el código no se puede reutilizar una función tan importante como es el calcular el recargo por riesgo de un paciente, ya que al tener toda esta lógica dentro de la función emitir(), que es gigante, se hace imposible que otro módulo que ocupe este calculo pueda obtenerlo|(legado.py:57)|Si esto no se corrige, va a obligar el duplicar codigo por si otro módulo necesita este calculo, ya que es imposible solo obtener ese calculo de la función gigante de emitir()|
| 6 | Reusar lo existente |Este principio lo rompe la función validar_cedula(), al realizar la validacion a "pata" de una cedula en vez de usar la biblioteca ya existente, rompiendo este principio al no reusar lo ya existente |(legado.py:154) |Si no se corrige pueden llegar a ocurrir huecos de seguridad, que en una biblioteca ya existente esto no puede suceder, además de perder tiempo creando algo ya existente y haciendo que puedan ocurrir errores que no se hayan tomado en cuenta.|
| 7 | Diseñar para la flexibilidad |Este principio se rompe ya que no existe la flexibilidad en el método de emitir(), haciendo que cada vez que se añada una famacia nueva se tenga que modificar el método.|(legado.py:78) |Cada vez que la empresa decida integrar una nueva cadena de farmacias al sistema, habrá que tocar y recompilar el núcleo del software, aumentando el riesgo de quebrar las integraciones de las farmacias que ya estaban funcionando bien.|
| 8 | Anticipar la obsolescencia |La obsolescencia no la está tomando en cuenta el código, ya que las Apis de las farmacias mueren y cambian con el tiempo, y al tener URLs fijas este principio no se cumple, ya que con el tiempo estas URLs van a dejar de funcionar.|(legado.py:30)|El día que una de las farmacias cambie su API, el sistema entero va a sufrir un colapso porque el código está amarrado rígidamente a esa implementación específica, haciendo que el costo de reemplazarla sea carísimo.|
| 9 | Diseñar para la portabilidad |Este principio se rompe al contener rutas de archivos con formatos rigidos, en la función de exportar se puede observar una ruta de windows, haciendo que en otros OS no llegue a funcionar este programa.|(legado.py:169)|Si la aplicación se intenta desplegar en un servidor Linuxel código se va a caer inmediatamente al intentar buscar una ruta de disco que no existe o usar sintaxis incompatible con el sistema operativo anfitrión.|
| 10 | Diseñar para la testabilidad |El código carece totalmente de testabilidad porque las dependencias críticas y no deterministas están incrustadas y se construyen por dentro, impidiendo que un programa externo pueda controlarlas u observarlas sin tocar el mundo real. Esto lo podemos ver en el código al tener funciones como el datetime.no(), el random.randint() y la conexión directa a SQLite.|(legado.py:50), (legado.py:66) y (legado.py:69) |Será completamente imposible escribir pruebas unitarias automatizadas y confiables que corran sin red, sin base de datos real y sin esperar el paso del tiempo, lo que convierte la verificación del sistema en un proceso manual y doloroso.|
| 11 | Diseñar defensivamente |Este principio no se cumple al emplear aserciones para validar datos que vienen del exterior, y además se utiliza una política de excepciones vacias, es decir, tenemos un error/excepción y no sabemos que fue lo que paso, al simplemente hacer "pass", esto sucede en el método de emitir() |(legado.py:62-63) y (legado.py:104) |En un entorno de producción, el sistema aceptará datos inválidos sin filtrar, y si ocurre un fallo al registrar en la bitácora, este se ignorará silenciosamente, perdiendo trazabilidad y corrompiendo la integridad de los datos de la clínica.|
